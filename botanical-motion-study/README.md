# Botanical motion study — production proof, not the narrated edit

An original 20-second 3840×2160, 60 fps Remotion animation study: canopy growth, an underground camera move, moving root-water particles, leaf-vein flow, living tissue, stomatal motion, and a chloroplast close-up. All artwork is editable SVG/React. All animation uses the composition frame. Sound cues are derived from `src/events.json`.

This generic study contains NO uploaded voiceover, NO uploaded example footage, and NO full supplied script. It is not a reference-matched final video. The new reference has not been audiovisually reviewed in this runtime, and narration has not been aligned. Do not substitute this proof for the full production.

## Run
Use Node.js 22 and Python 3. `npm ci` (or `npm install` when no lockfile is present), then `python3 sound.py`, `npm run studio` or `npm run render`. Remotion 4.0.526 is pinned. Output is `out/Botanical_Motion_Study_4K60.mp4`. Browser installation is `npx remotion browser ensure` when necessary.

No font binaries are included. Typography uses Arial with the system sans-serif fallback. All sound is original synthesis; no reference audio was extracted. Numbers are not presented as measured narration timings.

## Continuation requirements
Inspect the actual designated reference, measure the complete supplied voiceover, and construct semantic cues before applying these original rigs to a full timeline. Preserve voice speed, pitch, wording, and order. Do not reuse the study's 20-second schedule as narration alignment. Final review must distinguish full decoding, sample-frame inspection, and actual audiovisual playback.
